# Required — AES-256-GCM key derivation (min 32 chars, use: openssl rand -base64 32)
MASTER_KEY=your-super-secret-master-key-min-32-chars-here

# Internal SQLite database path
DATABASE_URL="file:./data/openquery.db"

# API server port
API_PORT=3001

# Node environment
NODE_ENV=development
